import { Box } from "@mui/system";
import React from "react";

interface Props {
  title: string;
}

function Button({ title }: Props) {
  return (
    <>
      <Box></Box>
    </>
  );
}

export default Button;
